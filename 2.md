# Логирование — Полный Конспект для Собеседования QA Backend
## 1. Что такое логирование

Логирование — это запись событий, ошибок и действий приложения в процессе его работы.

Зачем нужно:

поиск причин багов (RCA),

анализ цепочки событий,

обнаружение ошибок, таймаутов, исключений,

понимание взаимодействия микросервисов,

отладка асинхронных процессов (Kafka, очереди).

## 2. Типы логов
Application logs

Логи самого приложения.
Примеры:

INFO Starting payment
ERROR DB connection timeout

Access logs (NGINX / Gateway)

Показывают запросы: кто, куда и с каким кодом.

GET /payments/123 200 15ms
POST /orders 500 120ms

System logs

ОС, контейнеры, инфраструктура:

/var/log

journalctl

docker logs

Business logs

Бизнес-события:

Payment validated
User registered

## 3. Уровни логирования
Уровень	Значение
DEBUG	Подробности для отладки
INFO	Нормальная работа
WARN	Нештатная ситуация
ERROR	Ошибка, требуется действие
FATAL	Критическая ошибка, сервис не работает
## 4. Где искать логи
Локально / сервер
/var/log/app.log
/var/log/nginx/access.log

Docker
docker logs <container>
docker logs -f <container>

Kubernetes
kubectl logs <pod>
kubectl logs -f <pod>
kubectl logs <pod> --previous

CI/CD

GitLab CI, Jenkins, GitHub Actions.

## 5. Популярные системы логирования
### 🟦 ELK (Elasticsearch + Logstash + Kibana)

Используется для централизованного поиска по логам.

Типичный запрос:

level:ERROR AND payment_id:123

### 🟪 Sentry

Система для ошибок (exceptions):

stacktrace,

группировка issues,

метрики появления ошибок.

### 🟫 Graylog

Лог-менеджер поверх Elasticsearch.

### 🟧 Grafana Loki

Современная лёгкая система для логов:

{app="payments"} |= "ERROR"

## 6. Как QA анализирует логи
1. Поиск по trace_id / correlation_id

Каждый запрос имеет ID:

request_id:827ac12f


Позволяет увидеть всю цепочку событий.

2. Поиск ошибок

Фильтры:

level:ERROR
"Exception"
"timeout"
"failed"
"Traceback"

3. Поиск по business-id

По:

payment_id

order_id

user_id

session_id

Пример:

payment_id:123

4. Анализ цепочки событий

Пример цепочки:

INFO create payment id=123
INFO sending to kafka…
ERROR KafkaTimeoutException
WARN retry wanted
ERROR payment 123 not processed

5. Анализ side-effects

Например:

WARN address is null, using default


→ UI не отправил поле.

## 7. Типичные вопросы на собеседовании по логированию
1. Сервис вернул 500 — что делать?

Найти request_id

Найти по нему ERROR в логах

Проверить stacktrace

Найти виноватый сервис

2. Как найти причину таймаута?

сравнить timestamp событий,

смотреть upstream/downstream сервисы,

искать WARN/ERROR вокруг этого времени.

3. Как проверить, что Kafka сообщение отправилось?

Поиск:

Produced message to topic payments-out

4. Как понять, что был rollback?
Rollback transaction for payment 123

5. Разница между Sentry и ELK
Sentry	ELK
Только ошибки	Все логи
Stacktrace	Мощный поиск
Группировка багов	Корреляция событий
Пример для фронта/мобилок	Backend-heavy


## 2.1 CI/CD: ключевые знания для тестировщика

### Что такое CI/CD?

- **CI (Continuous Integration, непрерывная интеграция):**  
  Автоматическое объединение изменений из разных веток/разработчиков в общий код.  
  Каждый коммит сразу проверяется — сборка, тесты, линт — чтобы не появлялись ошибки в общем продукте.

- **CD (Continuous Delivery/Deployment, непрерывная доставка/развёртывание):**  
  Автоматизированная поставка проверенного кода на тестовые/боевые среды, чтобы можно было быстро и безопасно выкатывать новые версии.

---

### Зачем тестировщику знать CI/CD?

- Видит где “падает” билд или тесты (логи, алерты)
- Может использовать автотесты в пайплайне сразу после коммита
- Находит информацию о релизе, артефактах, окружениях
- Проверяет корректность автоматических проверок: если тесты не покрывают баг — CI поможет увидеть это сразу

---

### Примеры инструментов

#### 1. **GitLab CI**
- Дефолтный инструмент для репозиториев GitLab
- Пайплайны (pipeline) на YAML: задаёшь стадии (build, test, deploy), шаги, переменные
- Просматриваешь результат прямо в веб-интерфейсе репозитория
- Поддержка отчётов покрытия, ручных задач и уведомлений

#### 2. **Jenkins**
- Старейший и очень гибкий CI-сервер
- Огромное количество плагинов, визуализация этапов, триггеры по времени/событию, интеграция с разными системами (GitHub, Docker, Jira)
- Форма пайплайна: Groovy-файл (Jenkinsfile) или визуальный UI

#### 3. **GitHub Actions**
- Встроенный CI/CD-сервис для GitHub
- Конфиг через workflow-файл на YAML (`.github/workflows/` в репозитории)
- Автоматизация запуска тестов, выката докера, деплоя на сервер или облако
- Очень гибкая настройка событий: по коммиту, пул-реквесту, таймеру, лейблу

---

### Как тестировщик может использовать CI/CD?

- Настраивать автоматический запуск автотестов после каждого коммита/мержа (unit, e2e, regression, security)
- Проверять логи пайплайна для анализа причин падения/билда (чаще всего сразу видно где именно ошибка)
- Видеть, прошли ли проверки перед выкладкой (Gatekeeper — не прошло тесты, значит релиз не делаем)
- Конфигурировать уведомления по падениям тестов, статусам пайплайна (например, Telegram, Email, Slack)
- Контролировать артефакты (созданные файлы, контейнеры, отчёты о покрытии), быстро найти нужную версию для тестирования

---

### Пример типового workflow
<details>
  <summary>GitHub Actions, минимальный тестовый workflow</summary>

```yaml
name: Check & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install requirements
      run: pip install -r requirements.txt
    - name: Run tests
      run: pytest
```
</details>

---

### На практике

- **Плюсы для тестировщика**: быстрое выявление дефектов, история прогонов, прозрачность статусов, меньше ручных действий.
- **Что важно знать**: пайплайн плохо проходится — надо разбирать, насколько автотесты надёжны, не замыкается ли на качество только одного этапа (например, если ручные тесты не учтены).

---

## Простой пример заполнения `.gitlab-ci.yml` для автоматизации тестов в GitLab CI

### Основные части файла

- **stages:** этапы пайплайна (например, build, test, deploy)
- **jobs:** отдельные задачи для каждого этапа  
  Внутри каждой — шаги исполнения (скрипты), параметры, окружение

---

### Пример минимального `.gitlab-ci.yml`

```yaml
stages:
  - test         # Объявляем этапы пайплайна

test-job:
  stage: test    # К какому этапу относится джоб
  image: python:3.11     # Docker-образ, где будет запускаться задача
  script:               # Список команд, исполняемых в джобе
    - pip install -r requirements.txt
    - pytest
  tags:
    - ci        # (опционально: тэг раннера, если используется)
```

---

### Описание шагов

1. **stages**
   ```yaml
   stages:
     - test
   ```
   — Здесь у нас только этап тестирования (можно добавить build, deploy и любые другие).

2. **job (`test-job`)**
   - **stage:** этап (должен совпадать с одним из stages)
   - **image:** docker-образ, чтобы задать ПО/среду (например, python:3.11)
   - **script:** список shell-команд — что нужно сделать (установить зависимости, запустить автотесты)
   - **tags:** (опционально) нужен, если настроено несколько раннеров с разными правами

---

### Чуть сложнее: два этапа — тесты и сборка

```yaml
stages:
  - build
  - test

build-job:
  stage: build
  image: python:3.11
  script:
    - echo 'сборка проекта'   # здесь могла бы быть любая операция сборки
    - python setup.py build

test-job:
  stage: test
  image: python:3.11
  script:
    - pip install -r requirements.txt
    - pytest
```

---

### Еще фишки GitLab CI

- Можно добавить `only`/`except`, чтобы джоб запускался только на определённой ветке:
    ```yaml
    only:
      - master
    ```
- Можно использовать переменные окружения:
    ```yaml
    variables:
      MY_ENV_VAR: "значение"
    ```
- Можно сохранять артефакты (например, отчёты о тестах):
    ```yaml
    artifacts:
      paths:
        - reports/
    ```

---

### Итог

Весь .gitlab-ci.yml строится из:
- этапов (stages)
- задач (jobs: имя + шаги)
- (опционально) образа, переменных, артефактов, условий вызова

Базовый вариант для автотестов — один этап `test`, одна задача (job) внутри, в которой просто запускается `pytest`.  
Дальше по мере роста проекта можно добавлять сборку, деплой, анализ покрытий и пр.


## 2.3 Контейнеризация: основы для тестировщика на примере Docker

### Что такое контейнер и Docker?

**Контейнер** — это "упакованная" изолированная среда, где приложение запускается со всеми нужными зависимостями (библиотеками, конфигами, сервисами).  
**Docker** — популярнейший инструмент для работы с контейнерами, позволяет запускать, собирать, управлять ими быстро и просто.

---

### Зачем тестировщику уметь с контейнерами?

- Запускать приложения в "чистой" среде, не засоряя ОС
- Проверять работоспособность на разных версиях ПО (например, Python 3.10 vs Python 3.11)
- Воспроизводить баги из сборки, максимально похожей на production
- Уметь деплоить автотесты как сервис (например, запускать их в Jenkins/GitLab CI внутри docker-контейнера)
- Оперативно поднимать локальные сервисы для интеграционных тестов (БД, кэш, API и др.)

---

### Основные команды Docker

1. **Запуск контейнера:**
   ```bash
   docker run -it --rm python:3.11 bash
   ```
   — Запустит консоль bash с python 3.11. После выхода контейнер удалится (`--rm`).

2. **Проверить список контейнеров и образов:**
   ```bash
   docker ps           # Текущие контейнеры
   docker ps -a        # Все контейнеры (в т.ч. завершённые)
   docker images       # Скачанные образы
   ```

3. **Пробросить порт (для веб-приложений):**
   ```bash
   docker run -d -p 8080:80 nginx
   ```
   — Ваш локальный порт 8080 будет связан с сервисом nginx внутри контейнера на порту 80.

4. **Остановить контейнер:**
   ```bash
   docker stop <container_id>
   ```

---

### Пример автотестов в Docker

**Dockerfile** — рецепт (инструкция) как собрать собственный образ:

```dockerfile
FROM python:3.11

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY ../../../AppData/Roaming/JetBrains/PyCharm2024.3/scratches .

CMD ["pytest"]
```

**Запуск в один шаг:**
```bash
docker build -t my-app-test .
docker run my-app-test
```

---

### Взаимодействие с готовыми контейнерами в автотестах

- Например, для интеграционных тестов можно быстро поднять тестовую БД:  
  ```bash
  docker run --name postgres-test -e POSTGRES_PASSWORD=secret -d postgres:15
  ```
- В тесте указываешь параметры подключения:
    ```python
    db_host = "localhost"  # или внешний порт docker
    db_port = "5432"
    ```

---

### Docker Compose для нескольких контейнеров

Если нужно поднять сразу несколько сервисов (например, веб-приложение + бд + кэш):

**docker-compose.yml**
```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
```
Запуск:
```bash
docker-compose up
```

---

### Практические советы

- Всегда ищи готовый образ на [Docker Hub](https://hub.docker.com/) — не обязательно собирать все вручную!
- После теста контейнер можно удалить или сохранить для воспроизведения бага.
- Используй volume для сохранения данных между перезапусками:
  ```bash
  docker run -v ./mydata:/data app
  ```

---

**Итог:**  
Контейнеризация позволяет быстро и повторяемо собирать, запускать, тестировать любые проекты без "танцев с окружением". Для тестировщика — это ключ к стабильности интеграций и удобству регресса.

## Практический мини-гайд для тестировщика: тесты и ручная проверка через Docker

---

### Шаг 1. Собрать и запустить автотесты в Docker

1. **Создай файл `Dockerfile` в папке с проектом:**

```dockerfile
FROM python:3.11

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["pytest", "--maxfail=2", "--disable-warnings"]  # автотесты запускаются автоматически при старте контейнера
```

2. **Собери Docker-образ:**
```bash
docker build -t my-tests .
```
*(можешь выбрать любое имя)*

3. **Запусти контейнер с тестами:**
```bash
docker run --rm my-tests
```
- Все результаты тестов будут видны в консоли.
- После завершения контейнер удалится автоматически (`--rm`).

---

### Шаг 2. Поднять связку для ручного теста через Docker Compose (например, веб-приложение + база)

1. **Создай `docker-compose.yml`:**

```yaml
version: '3.8'
services:
  app:
    build: .           # берёт Dockerfile в текущей директории
    ports:
      - "8000:8000"    # проброс: локальный 8000 на 8000 в контейнере
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: test_db
    ports:
      - "5432:5432"
```

*(в примере поднимается Python-приложение на 8000 и PostgreSQL на 5432)*

2. **Запуск всех сервисов:**
```bash
docker-compose up
```

- Откроется доступ к приложению через http://localhost:8000  
- База данных будет доступна для ваших тестов/ручной работы
- Можно подключаться утилитами, SQL IDE, Postman и т.д.

---

### Шаг 3. Использование данных между перезапусками (volumes)

- Если нужно сохранить БД между тестами:  
  В секции db компонента в `docker-compose.yml` добавь:
  ```yaml
  volumes:
    - ./db_data:/var/lib/postgresql/data  # данные сохраняются на диске
  ```

---

### Шаг 4. Проверка связки вручную

1. Открой http://localhost:8000 (или нужный порт) в браузере, Postman, curl.
2. Используй любые привычные инструменты для проверки ручной работы:  
   - SQL IDE для подключения к базе
   - Инструменты логирования или консоли из контейнера
3. Для входа внутрь контейнера:
   ```bash
   docker exec -it имя_контейнера bash
   ```
   — можно посмотреть логи, вручную запускать команды, тестировать сервисы.

---

### Шаг 5 (дополнительный): Отчёты о тестах

- Если нужны отчёты, добавь в `Dockerfile`:
  ```dockerfile
  CMD ["pytest", "--maxfail=2", "--disable-warnings", "--junitxml=report.xml"]
  ```
  — отчёт сохранится как `report.xml`, можно пробросить volume чтобы увидеть его на хосте.

---

### Итог

- Всё настроено — тесты запускаются “чисто”, нет проблем с локальным окружением.
- Связку сервисов держишь под контролем, легко пересоздаёшь или делишься с коллегой: достаточно отдать Dockerfile/docker-compose.yml.

---

## 2.4 Брокеры сообщений: зачем нужны и как используются (Kafka, RabbitMQ)

---

### Что это такое?

**Брокер сообщений** — система для передачи данных между разными сервисами/приложениями по принципу "отправил — забыл".  
Он принимает сообщения от одного компонента (*producer*), хранит/буферизует их, и доставляет другому компоненту (*consumer*) — при этом отправитель и получатель могут работать независимо и в разное время.

---

### Зачем нужны брокеры сообщений?

- **Асинхронность**: Нет необходимости ждать, пока получатель обработает данные — система работает быстрее.
- **Масштабируемость**: Легко добавлять новых получателей или производителей сообщений без изменения архитектуры.
- **Надежность**: Брокер временно сохраняет сообщения — не потеряются, если сервис временно недоступен.
- **Декуплинг компонентов**: Меньше связей между частями системы, проще поддерживать и обновлять.
- **Очереди заданий**: Можно ставить задачи в очередь (например, обработка e-mail, отчётов, файлов).

---

### Примеры использования (RabbitMQ, Kafka)

#### **RabbitMQ**
- **Очереди задач** для фоновой обработки ("mail_job", "report_generation")
- **Обработка заказов**: веб-магазин — заказы поступают в очередь, воркеры берут задания в свободное время
- **Передача событий между сервисами**: веб-приложение отправляет событие, обработчик ловит и реагирует (логирование, уведомления)

**Пример схемы RabbitMQ:**
- Приложение отправляет сообщение:  
  ```python
  channel.basic_publish(exchange='', routing_key='calc_queue', body='calculate tax for user 7')
  ```
- Один или много обработчиков получают задачу:
  ```python
  for msg in queue:
      process(msg)  # начинаем обсчитывать налог
  ```
- Если пришло много задач — просто добавьте воркеры, они автоматически возьмут задачи из очереди.

---

#### **Kafka**
- **Хранилище событий для больших данных**: передача событий о кликах на сайте, транзакциях
- **Реализация архитектуры Event Sourcing**: любое изменение/действие пишется как событие в Kafka, любой сервис может "подписаться" и обрабатывать свою логику
- **Аналитика в реальном времени**: финансовые сервисы, IoT, трекинг пользователей, обработка логов

**Пример схемы Kafka:**
- Сервис пишет события:
    ```python
    producer.send('events', value={"user_id": 42, "action": "pay"})
    ```
- Другой сервис читает события (например, для расчёта налогов):
    ```python
    for msg in consumer:
        handle_event(msg)
    ```
- Можно читать события хоть с начала истории — удобно для аудита, регресса, аналитики.

---

### Как тестировщик может использовать брокеры

- Уметь ставить задачи в очередь вручную (отправить тестовое сообщение, проверить реакцию обработчика)
- Смотреть, что сообщения не теряются (тест на устойчивость, проверку ретраев)
- Проверять работу системы при высоких нагрузках (много сообщений — все ли обработаны)
- Аудит: благодаря брокеру всегда можно восстановить хронологию событий
- В тестах удобно мокать (эмулировать) сообщения для интеграционных проверок без запуска всех микросервисов

---

### Итог

**Брокер сообщений — ключ к устойчивым, гибким и масштабируемым системам, особенно когда много микросервисов**.  
RabbitMQ — проще, лучший вариант для очередей и фоновых задач.  
Kafka — мощнее, подходит для потоков событий и больших данных.

## Разбор самых нужных понятий и отличий для Kafka

---

### 1. Схема Kafka на картинке (простое описание)

**Kafka** — это потоковая система с темами (topics).
- **Producer** пишет события в topic (например, "tax_events").
- **Consumer Group** читает из topic эти сообщения (может быть несколько независимых групп).
- Внутри topic — партиции (разделы), чтобы данные могли читаться параллельно.

```
+-----------+         +---------+         +-----------+
| Producer  |  --->   |  Topic  |  --->   | Consumer  |
+-----------+         +---------+         +-----------+
                        |    |                 ^
  (события)             |----| (partition)     |
                        |    |                 |
                 <------- Consumer Group ------->
```

---

### 2. Что такое offset?

**Offset** — это “номер” (индекс) сообщения в партиции.
- Когда consumer читает сообщения, он запоминает offset последнего прочитанного сообщения.
- При сбое/перезагрузке consumer продолжит чтение с нужного места, не пропустив и не задвоив события.
- Offset независим для каждой consumer group!

---

### 3. Что такое Consumer Group?

**Consumer Group** — группа consumer-ов, которые делят между собой чтение сообщений из одного topic-а.
- Если сообщений много — читают параллельно (каждый consumer берет свою партицию).
- **Каждая группа** имеет собственный прогресс: сообщения, прочитанные одной группой, не влияют на другие.
- Позволяет делать отдельную логику (например, одна группа считает налоги, другая — рисует графики).

---

### 4. Чем Kafka отличается от RabbitMQ?

|              | Kafka                         | RabbitMQ                        |
| -------------| -----------------------------| --------------------------------|
| **Тип**      | Лог событий (Event log)      | Очередь (Queue)                 |
| **Память**   | Сообщения могут храниться долго (дни) | Сообщения исчезают после обработки |
| **Чтение**   | Можно читать сообщения несколько раз, с любого offset | Сообщение получают только один consumer |
| **Масштаб**  | Высокая пропускная способность, big data | Простые очереди, небольшие и средние данные |
| **Сценарий** | Хранение истории, аналитика, event sourcing | Отправка фоновых задач, очереди заданий |
| **Конс.группы** | Да (независимое чтение групп) | Нет (одно сообщение — один получатель) |

---

### 5. Как тестировать Kafka вручную

- **Простой ручной тест:**
    1. Запустить Kafka локально (через docker или дистрибутив).
    2. Отправить тестовое сообщение в topic:
        ```bash
        kafka-console-producer --broker-list localhost:9092 --topic tax_event
        ```
        Ввести строку:  
        ```
        {"user_id":123, "income":2000}
        ```
    3. Прочитать сообщение через consumer:
        ```bash
        kafka-console-consumer --bootstrap-server localhost:9092 --topic tax_event --from-beginning
        ```
    4. Проверить, что сообщение пришло.

- **Проверить consumer group:**
    - Запусти несколько consumer-ов с одинаковой group.id — увидишь, что они “делят” партиции!

---

### 6. Как тестировать Kafka через автотесты (Python)

```python
from kafka import KafkaProducer, KafkaConsumer

def test_send_and_receive_kafka():
    producer = KafkaProducer(bootstrap_servers="localhost:9092")
    consumer = KafkaConsumer(
        'test_topic',
        bootstrap_servers="localhost:9092",
        group_id="test-group",
        auto_offset_reset='earliest'
    )
    producer.send('test_topic', b'hello')
    producer.flush()
    for msg in consumer:
        assert msg.value == b'hello'
        break
```

---

**Если нужна картинка-схема, docker-compose для быстрой локальной проверки Kafka, готовый пример — напиши!**

## 2.5 База данных: основные понятия, сравнение SQL/NoSQL и пример запросов

---

### 1. Основные понятия

- **База данных (БД)** — организованное хранилище информации, которую можно быстро искать, изменять.
- **Таблица** — набор данных в виде строк и столбцов (пример: User, Order).
- **Строка** — одна запись (например, один пользователь).
- **Столбец** — отдельный параметр (имя, почта, доход).
- **Ключ (Primary key)** — уникальный идентификатор (user_id).
- **Внешний ключ (Foreign key)** — ссылка на другую таблицу (user_orders: user_id → users.id).
- **Индексы** — ускоряют поиск по БД для больших данных.

---

### 2. Виды баз данных

#### SQL (реляционные базы)
- Структурированные, строго типизированные таблицы.
- Запросы через язык SQL (Structured Query Language).
- Поддерживают транзакции, связи между таблицами и строгие типы.
- Примеры: **PostgreSQL, MySQL, Oracle, MS SQL Server**

#### NoSQL (нереляционные базы)
- Гибкая структура: документы, ключ-значение, граф, столбцы.
- Не всегда есть строгая схема таблицы.
- Быстро работает с JSON, большими потоками, часто используется для масштабируемых web-приложений.
- Примеры: **MongoDB, Redis, Cassandra, DynamoDB**

---

### 3. Примеры SQL-запросов для PostgreSQL/MySQL

#### Выбрать все строки
```sql
SELECT * FROM users;
```
— даст всю таблицу пользователей.

#### Выбрать пользователей с доходом больше 2000
```sql
SELECT id, name, income FROM users WHERE income > 2000;
```

#### Добавить строку (нового пользователя)
```sql
INSERT INTO users (name, income) VALUES ('Ivan Ivanov', 3500);
```

#### Изменить значение дохода
```sql
UPDATE users SET income = 5000 WHERE name = 'Ivan Ivanov';
```

#### Удалить пользователя
```sql
DELETE FROM users WHERE id = 17;
```

#### Агрегация: средний доход
```sql
SELECT AVG(income) FROM users;
```

#### Соединение (JOIN) двух таблиц: найти всех заказы пользователя
```sql
SELECT u.name, o.order_id, o.amount
FROM users u
JOIN orders o ON u.id = o.user_id
WHERE u.name = 'Ivan Ivanov';
```

---

### 4. Кратко: SQL vs NoSQL

|         | SQL (реляционные)           | NoSQL (нереляционные)    |
| ------- | --------------------------- | ------------------------ |
| Структура | Чётко заданные таблицы        | Гибкие форматы: JSON, ключ-значение |
| Связи    | Много связей, JOIN-ы            | Обычно только ссылки или вообще без связей |
| Транзакции | Надёжные, есть ограничения    | Может нет строгих транзакций |
| Масштабирование | вертикально (большая машина) | горизонтально (много машин) |
| Примеры | PostgreSQL, MySQL              | MongoDB, Redis, Cassandra |

---

### 5. Для автотестов и ручных проверок

- SQL чаще используется для бизнес-данных (пользователи, финансы).
- NoSQL — для логов, больших объемов телеметрии, кэша.

**Для теста:**
```python
import psycopg2
conn = psycopg2.connect(...)
cur = conn.cursor()
cur.execute("SELECT income FROM users WHERE name='Ivan Ivanov'")
print(cur.fetchone())
```

---

## Redis: зачем нужен и базовое использование

---

### Что такое Redis?

**Redis** — это сверхбыстрая система хранения данных типа "ключ-значение" (NoSQL), которая полностью живёт в оперативной памяти (RAM).  
Популярен для кэшей, очередей заданий, временных сессий, хранения быстрых параметров.

---

### Зачем Redis?

- **Кэширование**: хранить результаты вычислений, часто используемые данные (например, профиль пользователя, токен, config).
- **Сессии/авторизация**: хранить данные о входе пользователей для быстрого доступа.
- **Очереди и pub/sub**: организовать очередь задач, подписку на события (например, временные сообщения для чата).
- **Счётчики, рейтинги, лимиты**: удобно хранить временные рейтинги, лимиты запросов (например, ограничение на число попыток, просмотров).
- **Мгновенные операции**: любые ключ-значение с миллионами запросов в секунду — для high-load проектов.

---

### Базовое использование Redis

#### Ручная работа через консоль

1. **Запустить сервер Redis**
   ```bash
   redis-server
   ```
   (или docker: `docker run -p 6379:6379 redis:7`)
2. **Подключиться клиентом**
   ```bash
   redis-cli
   ```
3. **Основные команды**
   ```bash
   SET user:7:income 5000               # записать значение
   GET user:7:income                    # получить
   INCR page_views                      # увеличить счетчик
   DEL user:7:income                    # удалить ключ
   EXPIRE user:7:income 60              # задать срок жизни 60сек.
   ```

---

#### В Python (через библиотеку `redis`)

```python
import redis

r = redis.Redis(host='localhost', port=6379, db=0)

r.set('user:7:income', 5000)
income = r.get('user:7:income')
print(int(income))       # 5000

r.incr('page_views')     # +1 к счетчику
r.delete('user:7:income')
```

---

### Примеры сценариев

- 👨‍💻 **Кэш профилей пользователей**
- 🔒 **Таймауты на авторизацию (пытаешься 5 раз — блокировка на 5 минут)**
- 💌 **Очередь сообщений в реальном времени (pub/sub)**
- 📈 **Быстрые статистики, расчёты в highload-проектах**

---

### Особенности Redis

- Всё в памяти — максимально быстрый доступ (миллисекунды)
- Умеет сбрасывать данные на диск (настройкой)
- Простая структура: ключ — значение (может быть строка, список, множество, хэш)
- Есть TTL (время жизни ключа)
- Кроссплатформенность (Linux, Windows, docker-образы)

---

**Итог:**  
Если приложению нужен быстрый кэш, счётчики или время жизни сессии — Redis подходит отлично. Для ручного тестирования — просто запусти сервер и работай через redis-cli.

## Реальный банковский пример использования Redis

**Сценарий: Лимит платежей и защита от повторной отправки**

---

### 1. Лимитирование операций (anti-fraud, rate-limiting)

Когда пользователь/клиент банка пытается провести много операций за короткое время, важно ограничивать частоту запросов для предотвращения мошенничества или спама.

**Пример логики**:  
- Разрешить не более N платежей в минуту с одного аккаунта.

**Реализация через Redis:**

```python
import redis
from time import time

r = redis.Redis(host='localhost', port=6379, db=0)

def can_pay(user_id):
    key = f"user:{user_id}:payments"
    current_count = r.get(key)
    if current_count and int(current_count) >= 5:  # лимит 5 платежей
        return False
    else:
        r.incr(key)
        r.expire(key, 60)   # ключ живёт 60 сек., потом обнуляется
        return True

# Применение: при попытке провести платёж
user_id = 42
if can_pay(user_id):
    print("Платёж проведён")
else:
    print("Превышен лимит. Попробуйте позже!")
```

---

### 2. Защита от дубля (idempotency)

Банки часто защищаются от повторных запросов — чтобы платеж не списался два раза при повторной отправке формы (например, из-за плохого интернета).

**Реализация через Redis:**

```python
def is_duplicate(transaction_id):
    key = f"txn:{transaction_id}"
    if r.exists(key):
        return True
    else:
        r.set(key, 1)
        r.expire(key, 600)   # ключ живёт 10 минут - проверка совершения операции
        return False

# Применение: при обработке платежа
txn_id = "A1B2C3"
if is_duplicate(txn_id):
    print("Повторный запрос, платеж уже проведён")
else:
    print("Проводим платеж")
```

---

### 3. Кэш баланса для ускорения операций

Вместо обращения к медленной основной базе, баланс считывается из Redis:

```python
balance = r.get('user:42:balance')
if balance is None:
    # Если нет в кэше — достать из БД, положить в кэш Redis
    balance = get_balance_from_db(42)
    r.set('user:42:balance', balance)
print(f"Баланс: {float(balance)}")
```

---

### Кратко

- **Лимиты** (по времени/числу операций),
- **Идемпотентность** (защита от повторов при сетевых сбоях),
- **Кэширование** (ускорение частых операций).

**Во всех реальных банковских сервисах Redis часто используется для этих задач — подключён к фронту, микросервисам, anti-fraud системам.**

## GIT — базовые команды для работы с репозиторием

---

### 1. Клонирование репозитория

**Копировать проект из удалённого (обычно GitHub/GitLab):**
```bash
git clone https://github.com/user/repo.git
```
— создаётся локальная копия репозитория.

---

### 2. Просмотр изменений, статус

**Показать, что изменилось:**
```bash
git status
```
— список новых, изменённых, удалённых файлов.

---

### 3. Добавить файлы в индекс (стадию для коммита)

```bash
git add filename.py         # только один файл
git add .                   # все изменения в папке

```

---

### 4. Зафиксировать изменения — сделать коммит

```bash
git commit -m "Добавлен расчёт налога"
```
— создаёт “снимок” изменений с комментарием.

---

### 5. Отправить коммит на сервер (push)

```bash
git push                    # если ветка настроена
git push origin main        # на ветку main
```
— публикация новых коммитов на сервере (на GitHub, GitLab и т.д.)

---

### 6. Работа с ветками (branch)

- Список всех веток:
  ```bash
  git branch
  ```
- Создать новую ветку:
  ```bash
  git branch feature/new-tax
  ```
- Переключиться на ветку:
  ```bash
  git checkout feature/new-tax
  ```
- Создать и сразу перейти:
  ```bash
  git checkout -b feature/new-tax
  ```

---

### 7. Слить ветки (merge)

- Переключиться на ветку, в которую сливаем (например, main):
  ```bash
  git checkout main
  ```
- Слить изменения из другой ветки (feature/new-tax):
  ```bash
  git merge feature/new-tax
  ```
— все изменения объединяются.

---

### 8. Получить последние изменения с сервера (pull)

```bash
git pull
```
— обновить свой локальный репозиторий.

---

### Кратко — полный цикл

1. Клонировал репозиторий
2. Создал ветку для своей задачи (feature)
3. Внёс правки, добавил (`git add`)
4. Зафиксировал (`git commit`)
5. Отправил (`git push`)
6. Объединил результат с основной веткой (`git merge`)

---

