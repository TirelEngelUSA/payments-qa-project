## ЭТАП 3. Основы Backend, API/тестирование  
### HTTP/HTTPS: что это, структура, отличия, методы

---

### Что такое HTTP и HTTPS?

- **HTTP (HyperText Transfer Protocol)** — протокол передачи данных между клиентом (например, браузер, Postman) и сервером (backend).
- **HTTPS** — это HTTP + шифрование через SSL/TLS. Все данные между клиентом и сервером защищены (невозможно "подслушать" содержимое в сети).

---

### Структура HTTP-запроса

**Запрос содержит:**
- Метод (GET, POST, PUT, DELETE и др.)
- URL (адрес ресурса)
- Headers (заголовки: тип данных, авторизация и др.)
- Body (тело запроса — обычно в POST/PUT)

**Пример:**
```
POST /api/calc_tax HTTP/1.1
Host: example.com
Content-Type: application/json
Authorization: Bearer abc123

{
  "income": 4000
}
```

**Ответ:**
```
HTTP/1.1 200 OK
Content-Type: application/json

{
  "tax": 210
}
```

---

### Чем отличается HTTPS от HTTP?

- **HTTPS** шифрует данные — защита от перехвата/взлома, обязательно для веб-банков, e-commerce, всего, где есть приватные данные.
- Для HTTPS нужен сертификат и специальная настройка сервера.

---

### Основные методы HTTP

| Метод   | Описание                                   | Применение                       |
|---------|--------------------------------------------|----------------------------------|
| GET     | Получить данные                            | Чтение информации (список, деталь)|
| POST    | Создать новые данные                       | Добавление записи, регистрация    |
| PUT     | Изменить полностью (или создать)           | "Обновить" объект                 |
| PATCH   | Частично изменить                          | Изменить 1-2 поля объекта         |
| DELETE  | Удалить данные                             | Стирает объект                    |
| HEAD    | Получить только заголовки                  | Проверить состояние               |
| OPTIONS | Узнать, какие методы поддерживает ресурс   | Предварительный запрос            |

**Примеры:**
- Получить пользователя:  
  ```http
  GET /users/17
  ```
- Создать пользователя:  
  ```http
  POST /users
  ```
- Изменить email пользователя:  
  ```http
  PATCH /users/17
  ```
- Удалить пользователя:  
  ```http
  DELETE /users/17
  ```

---

### Для тестирования API

- Используются инструменты: **Postman, curl, автотесты на Python (requests)**
- Проверять нужно:  
  - правильность структуры запроса/ответа  
  - коды ответа (200 — успех, 400 — ошибка клиента, 500 — ошибка сервера)  
  - безопасность (HTTPS, авторизация)

---

## Примеры HTTP-запросов через curl и Postman

---

### 1. Получить список пользователей (GET)

**curl:**
```bash
curl -X GET "https://api.example.com/users"
```

**Postman:**
- Метод: `GET`
- URL:    `https://api.example.com/users`
- Params: (можно добавить фильтры или пагинацию, например ?page=2)

---

### 2. Получить конкретного пользователя по id (GET)

**curl:**
```bash
curl -X GET "https://api.example.com/users/17"
```

**Postman:**
- Метод: `GET`
- URL:   `https://api.example.com/users/17`

---

### 3. Создать пользователя (POST c JSON body)

**curl:**
```bash
curl -X POST "https://api.example.com/users" \
  -H "Content-Type: application/json" \
  -d '{"name": "Ivan", "income": 3500}'
```

**Postman:**
- Метод: `POST`
- URL:   `https://api.example.com/users`
- Body: (raw, формат JSON):
```json
{
  "name": "Ivan",
  "income": 3500
}
```
- Headers:  
  Content-Type: application/json

---

### 4. Изменить доход пользователя (PATCH)

**curl:**
```bash
curl -X PATCH "https://api.example.com/users/17" \
  -H "Content-Type: application/json" \
  -d '{"income": 4000}'
```

**Postman:**
- Метод: `PATCH`
- URL:   `https://api.example.com/users/17`
- Body (raw, JSON):
```json
{
  "income": 4000
}
```
- Headers:  
  Content-Type: application/json

---

### 5. Удалить пользователя (DELETE)

**curl:**
```bash
curl -X DELETE "https://api.example.com/users/17"
```

**Postman:**
- Метод: `DELETE`
- URL:   `https://api.example.com/users/17`

---

### 6. Запрос с авторизацией (Bearer Token)

**curl:**
```bash
curl -X GET "https://api.example.com/secure/data" \
  -H "Authorization: Bearer abc123"
```

**Postman:**
- Метод: `GET`
- URL:   `https://api.example.com/secure/data`
- Headers:  
  Authorization: Bearer abc123  
(можно добавить в Authorization tab "Bearer Token" поле)

---

### 7. Отправить файлы (POST + multipart)

**curl:**
```bash
curl -X POST "https://api.example.com/upload" \
  -F "file=@photo.jpg"
```

**Postman:**
- Метод: `POST`
- URL:   `https://api.example.com/upload`
- Body:  form-data  
  key: file, value: (выбери файл)

---

### 8. Пользовательский GET с параметрами фильтрации (например, income > 5000)

**curl:**
```bash
curl -X GET "https://api.example.com/users?income_min=5000"
```

**Postman:**
- Метод: `GET`
- URL:   `https://api.example.com/users`
- Params:  
  income_min = 5000

---

### 9. Запрос только заголовков (HEAD)

**curl:**
```bash
curl -I "https://api.example.com/users/17"
```

**Postman:**
- Метод: `HEAD`
- URL:   `https://api.example.com/users/17`

---

#### Комментарий

- В Postman всё вводится почти так же, только более удобно — просто выбираешь метод, url, добавляешь тело/параметры/заголовки.
- curl часто используют в автотестах, скриптах, CI/CD, а Postman — для ручных исследований и проверки API.

#️⃣ API: базовые знания для QA

(REST, SOAP, GraphQL, Socket + тестирование)

1. Что такое API (простыми словами)

API — это способ общения между приложениями.
Сервис А спрашивает/передаёт что-то сервису B.

Типичный QA кейс:

Отправляем запрос → получаем ответ → проверяем, что всё корректно.

API бывают разные. Ниже — 4 самых важных.

2. REST API

REST — наиболее распространённый тип API на сегодня.

Ключевые идеи:

работает поверх HTTP

использует методы:
GET, POST, PUT, PATCH, DELETE

данные чаще всего в JSON

понятные URL
пример:
/users/123/payments?limit=20

Пример запроса (curl):
curl -X GET https://api.example.com/users/123

Пример ответа:
{
  "id": 123,
  "name": "Alex",
  "active": true
}

Как тестировать:

Postman / Insomnia

curl

автотесты (pytest + requests, Java + RestAssured)

3. SOAP API

Более старый стандарт. Всё строго.

Особенности:

формат данных: XML

операции описаны контрактом — WSDL

строгие схемы, строгие правила

может быть сложнее для отладки

Пример SOAP-запроса:
<soapenv:Envelope>
   <soapenv:Body>
      <getUser>
         <id>123</id>
      </getUser>
   </soapenv:Body>
</soapenv:Envelope>


Сегодня SOAP встречается:

в банках

госуслугах

старых корпоративных сервисах

4. GraphQL

Современная альтернатива REST.

Особенности:

один URL, но разные запросы

клиент сам выбирает какие поля нужны

возвращает только то, что запросили

Пример запроса:
query {
  user(id:123) {
    id
    name
    payments(limit:3){
      id
      amount
    }
  }
}

Пример ответа:
{
  "data": {
    "user": {
      "id": 123,
      "name": "Alex",
      "payments": [
        {"id": 1, "amount": 100},
        {"id": 2, "amount": 200},
        {"id": 3, "amount": 300}
      ]
    }
  }
}


Очень удобен для фронта.

5. WebSocket / Socket API

Используется когда нужно «real time».

Отличия от REST:

соединение постоянное, не разовое

сервер может присылать данные сам

используют чаты, биржи, игры

Пример сообщения WebSocket:

Client → Server: {"subscribe":"BTC_PRICE"}
Server → Client: {"price": 68000.52}


Тестирование:

wscat

websocket clients в браузере

автотесты через ws библиотеки

6. Как тестировать API
📌 6.1. Postman

Самый удобный инструмент.

Что можно:

отправлять запросы

параметризовать переменные

делать коллекции тестов

автоматические проверки (test scripts, pm.expect)

📌 6.2. curl

Нужен QA, чтобы проверять быстро через терминал.

curl -X POST https://api.example.com/login \
     -H "Content-Type: application/json" \
     -d '{"user":"alex", "pass":"123"}'

📌 6.3. Автотесты (Python пример)
import requests

def test_user():
    r = requests.get("https://api.example.com/users/123")
    assert r.status_code == 200
    assert r.json()["id"] == 123


В прод-компаниях (где microservices) API тесты — основной тип тестов.

7. Что должен уметь QA касательно API
Минимум:

✔ отправить запрос (Postman/curl)
✔ посмотреть ответ (JSON)
✔ разобрать заголовки
✔ понять коды ответов (200, 400, 401, 403, 404, 500)
✔ проверить логи сервиса
✔ написать простые API тесты

Средний уровень:

✔ тестировать авторизацию / токены
✔ работать с swagger / API документацией
✔ автотесты с моками
✔ тестировать ошибки и граничные случаи

Продвинутый QA:

✔ поднимать среду через Docker
✔ тестировать Kafka/WebSockets
✔ тестировать интеграции (S3, Redis, DB)
✔ знать контрактное тестирование

