## 1.1 Теория тестирования — основные виды тестов

### Цель:  
Понять, какие бывают типы тестирования, зачем каждый нужен, где используется и чем отличаются.

---

### КЛЮЧЕВЫЕ ВИДЫ ТЕСТИРОВАНИЯ

#### 1. Функциональное тестирование  
— Проверяет, что система работает в соответствии с требованиями:  
- Вводишь input → ожидаешь конкретный output  
- Например: отправляешь форму — данные должны сохраниться

**Когда применяется:**  
- Проверка функционала новых/изменённых частей системы  
- Работа в ручном и автоматическом режиме

**Пример:**  
- Проверить, что клиент может успешно сделать перевод денег — ввести сумму, номер получателя, подтвердить перевод, увидеть сообщение об успешной операции.
- Проверить, что работает авторизация: неправильный пароль — ошибка, правильный пароль — вход.

---

#### 2. Регрессионное тестирование  
— Проверяет, что новые изменения в ПО не сломали то, что работало ранее

**Когда применяется:**  
- После интеграции новых фич, багфиксов  
- Автоматизация — часто автотестами

**Пример:**  
- После добавления нового способа оплаты убедиться, что старые способы (перевод между счетами, пополнение через карту) продолжают работать без ошибок.
- После исправления бага с отображением выписки — все другие формы и отчеты всё ещё отображаются корректно.
---

#### 3. Интеграционное тестирование  
— Проверяет взаимодействие между различными компонентами системы

**Пример:**  
- Как сервис оплаты работает вместе с системой авторизации  
- Как два микросервиса обмениваются данными
- Проверить, что после перевода на другой банк платеж корректно отображается в личном кабинете и подтверждается внешним API банка-получателя.
- Проверить совместную работу модуля автоплатежей и системы уведомлений (при ошибке — приходит оповещение).

---

#### 4. Системное тестирование  
— Проверяет работу всей системы целиком в реальных условиях

**Что тестируют:**  
- Все функции  
- Интеграции  
- Нагрузку (performance)  
- Безопасность

**Пример:**  
- Провести комплексное тестирование всего интернет-банка: вход, переводы, выписки, оплата услуг — всё вместе, как конечный пользователь.
- Проверить работу мобильного приложения и веб-версии банка с одной учетной записью.

---

#### 5. Приемо-сдаточное тестирование (Acceptance testing)  
— Тестирование ПО для проверки, выполняет ли оно требования заказчика

**Виды:**  
- User Acceptance Testing (UAT) — пользователь подтверждает, что продукт «годен»
- Alpha/Beta testing

**Пример:**  
- Заказчик (не тестировщик!) проверяет, что реализованная функция "автоплатеж ЖКХ" работает в интерфейсе как ему требуется.
- Пользователь тестирует новый сервис переводов и подтверждает, что UI, скорость и удобство соответствуют ожиданиям.

---

#### 6. Нагрузочное тестирование (Performance/Stress testing)  
— Проверка, как система работает под большой нагрузкой

**Примеры:**  
- Какой максимум запросов выдержит сервер  
- Что произойдет при наплыве пользователей
- Запустить 1000 одновременных логинов и переводов — смотрим, падает ли система, возникает ли задержка.
- Проверить, справляется ли сервер с расчетами по всем кредитным счетам ночью, когда проходит массовая обработка.

---

#### 7. Smoke/Sanity testing  
— «Дымовое» тестирование — базовая проверка, что основной функционал работает и система не «упала» после релиза.

**Когда:**  
- После релиза, развертывания  
- Перед более глубоким тестированием

**Пример:**  
- После выката релиза проверить: возможен ли вход в систему, отображается ли баланс, можно ли сделать перевод — если всё ок, приступаем к углубленному тестированию.
- Проверить базовые операции: авторизация, просмотр истории платежей, создание нового шаблона платежа.

---

#### 8. Секьюрити (Security) тестирование  
— Проверка безопасности системы: возможные уязвимости, утечки данных и др.

**Пример:**  
- Проверить, что попытка SQL-инъекции через форму авторизации блокируется или обрабатывается.
- Проверить, что пользователь не может получить доступ к чужому счету, даже если подменить параметры ID счета в запросе.
- Проверить, что все пароли в базе хранятся в зашифрованном виде.
---

### В итоге — что важно запомнить

- Каждый из видов тестирования — отдельный фокус, свой набор задач.
- На практике часто пересекаются и комбинируются.
- Главная цель: убедиться, что продукт работает корректно и соответствует ожиданиям заказчика.

---

## 1.2 Основы тест-дизайна — подробно и с банковскими примерами

### Цель:
Понимать, как правильно составлять тестовые сценарии, чтобы охватить максимально возможные баги с минимальным количеством тестов.

---
### Общая задача для тестирования

**Каждый пользователь должен оплатить в казну:**  
- 150 единиц автоматически (базовый налог)
- Если доход ОТ 1000 ДО 10000 — дополнительно 10 единиц с каждой ПОЛНОЙ или НЕПОЛНОЙ сотни СВЫШЕ минималки в 999  
  (т.е. за каждую сотню начиная с дохода 1000)
- Если доход СВЫШЕ 10000 — дополнительно 5% от дохода, округлить до целого значения



### Основные элементы тест-дизайна

#### 1. Классы эквивалентности (equivalence classes)
- Это группы значений, которые обрабатываются системой одинаково.
- Тестировать по одному представителю из каждого класса.
- **Пример для банка:**  
  - Поле «Сумма перевода»:  
    - Класс 1: отрицательные значения (невалидно, ошибка)
    - Класс 2: 0 (невалидно)
    - Класс 3: положительные значения до лимита (валидно)
    - Класс 4: значения больше лимита (ошибка)

```python
def calc_tax(income):
    min_tax = 150
    if income < 1000:
        return min_tax
    elif income <= 10000:
        # Берём только часть сверх 999: income - 999
        hundreds = ((income - 999) + 99) // 100  # округление вверх
        return min_tax + 10 * hundreds
    else:
        percent = round(income * 0.05)
        return min_tax + percent

# Классы
# <1000 — только минимум
# 1000..10000 — 10 ед/100
# >10000 — 5% + минималка

@pytest.mark.parametrize(
    "income,expected",
    [
        (800, 150),      # <1000, только базовый налог
        (2500, 310),     # ((2500-999)+99)//100 = 16 сотен; 150+10*16=210
        (12000, 750),    # 12000 > 10000, 0.05*12000=600; 150+600=750
    ]
)
def test_equivalence_classes(income, expected):
    assert calc_tax(income) == expected   
```
---

#### 2. Граничные значения (boundary values)
- Ошибки часто встречаются на границах диапазона.
- Надо отдельно тестировать: граничные, чуть меньше, чуть больше.
- **Пример для банка:**  
  - Лимит перевода — максимум 100 000 руб.
    - Проверить: 99 999 (валидно), 100 000 (валидно), 100 001 (ошибка)
    - Проверить: 0 (ошибка), 1 (валидно)
    - 
```python
@pytest.mark.parametrize(
    "income,expected",
    [
        (999, 150),      # Граница — переход на дополнительные сотни
        (1000, 160),     # Первая сотня сверх минималки
        (10000, 1060),   # Граница — максимальное количество сотен
        (10001, 650),    # Переход на процент
    ]
)
def test_boundary_values(income, expected):
    assert calc_tax(income) == expected
```
**Пояснение:**  
- 999 (до нижней границы — только минималка)
- 1000 (первая “сотня” сверх 999)
- 10000 (последний случай по сотням)
- 10001 (уже по проценту)

---

#### 3. Таблицы решений (decision tables)
- Использовать, если системное поведение зависит от нескольких параметров: если-ТО, когда-ТО.
- **Пример для банка:**  
  - Если тип счета = «валютный» и сумма < лимита — можно перевести  
  - Если тип счета = «рублевый» и сумма > лимита — нельзя  
  - В таблице видим все возможные сочетания: тип счета × сумма
  
| Доход      | Ожидание            |
|------------|---------------------|
| <1000      | 150                 |
| 1000       | 150 + 10×1 = 160    |
| 1050       | 150 + 10×1 = 160    |
| 1099       | 150 + 10×1 = 160    |
| 1100       | 150 + 10×2 = 170    |
| 1500       | 150 + 10×6 = 210    |
| 10000      | 150 + 10×91 = 1060  |
| 10001      | 150 + round(10001×0.05) = 650 |
| 15000      | 150 + round(15000×0.05) = 900 |

---

#### 4. Кейс (case)/сценарий (test scenario)
- Сценарий тестирования — последовательность действий, максимально повторяющая реальный пользовательский путь.
- **Пример для банка:**  
  - Вход в систему ➔ поиск получателя ➔ создание шаблона ➔ перевод ➔ подтверждение ➔ проверка операции в истории

---

#### 5. Чек-листы
- Краткие списки пунктов для проверки, без явного прописывания шагов.
- **Пример для банка:**  
  - Авторизация: правильный пароль/логин; неправильный пароль; пустые поля; спецсимволы  
  - Перевод: сумма минимальная/максимальная; перевод за границу; перевод самому себе

---

#### 6. Партицирование (разделение на подгруппы)
- Разделять наборы данных/сценариев так, чтобы протестировать их по подгруппам.
- **Пример для банка:**  
  - Разные типы пользователей: обычный клиент, сотрудник банка, администратор.
  - Разные типы переводов: на карту, на счет, за границу, между своими счетами.

---

#### 7. Использование данных тестирования
- Генерировать тестовые данные вручную или с помощью скриптов.
- **Пример для банка:**  
  - Клиенты с разным уровнем доступа, с разными статусами счетов (невалидный, заблокированный, активный).
```python
test_users = [
    {"id": 1, "income": 850, "expected": 150},        # меньше 1000
    {"id": 2, "income": 1000, "expected": 160},       # на границе
    {"id": 3, "income": 1500, "expected": 210},       # диапазон сотен
    {"id": 4, "income": 10000, "expected": 1060},     # максимум для сотен
    {"id": 5, "income": 12000, "expected": 750},      # процент
    {"id": 6, "income": 25000, "expected": 1400},     # процент, больше
    {"id": 7, "income": 0, "expected": 150},          # крайний невалидный
    {"id": 8, "income": -500, "expected": 150},       # отрицательный
    {"id": 9, "income": 999, "expected": 150},        # граничное значение
    {"id":10, "income": '2500', "expected": None},    # строка, нужно обработать ошибку (пример негативного теста)
]

@pytest.mark.parametrize("user", test_users)
def test_data_cases(user):
    # если income не int/float, тест должен упасть или вернуть None
    if not isinstance(user["income"], (int, float)):
        assert user["expected"] is None
    else:
        assert calc_tax(user["income"]) == user["expected"]
```
---

### Итог

- Грамотный тест-дизайн помогает быстро покрыть приложение хорошими тестами и экономить время.
- Классы эквивалентности и граничные значения — часто основа любых сценариев.
- Для банковских систем важно точно выделять случаи с лимитами, типами переводов, правами пользователя.


## 1.3 Тестовая документация: что, зачем и как вести

### Цель:
Научиться оформлять и использовать тестовую документацию, знать её виды, структуру и принципы написания.

---

### Зачем нужна тестовая документация?

- Фиксирует, что и как проверялось (история изменений, ретроспектива)
- Техническое задание для тестировщика и разработчика (все шаги и кейсы)
- Поддерживает команду: быстро обучает новых участников, упрощает регрессию
- Основа для анализа покрытия тестами, поиска "белых пятен"
- Помогает валидировать требования с бизнесом и заказчиком

---

### Основные виды тестовой документации

#### 1. **Тест-план (Test plan)**
- Общая стратегия тестирования проекта
- Риски, объём, ресурсы, требования к качеству
- Что, когда и чем будут тестировать, кто отвечает

#### 2. **Тестовые сценарии (Test scenarios) / кейсы**
- Последовательность реальных пользовательских действий
- Каждый сценарий — одна бизнес-функция, конец валидации = результат

#### 3. **Тест-кейсы (Test cases)**
- Формализованный блок: входные параметры, шаг, ожидание
- Часто оформляются в таблице/системе тест-менеджмента

##### Пример тест-кейса (задача про налог):

| № | Действие                  | Вход         | Ожидание        |
|---|---------------------------|--------------|-----------------|
| 1 | Ввести доход 800          | 800          | 150             |
| 2 | Ввести доход 1000         | 1000         | 160             |
| 3 | Ввести доход 9999         | 9999         | 1050            |
| 4 | Ввести доход 10001        | 10001        | 650             |
| 5 | Ввести некорректный доход | 'abc'        | Ошибка/отказ    |

---

#### 4. **Чек-листы**
- Простой список основных проверок: запускал — отметил
- Удобно для быстрых smoke, regression, приёмки

#### 5. **Баг-репорты (Defect/Bug report)**
- Структурированное описание проблемы/дефекта: шаги, ожидание, фактическое, серьезность, среда

#### 6. **Тестовая матрица / покрытие**
- Таблица соответствия кейсов и требований (что протестировано, а что нет)

---

### Краткая структура хорошей тестовой документации:

1. Название, идентификатор
2. Описание цели теста
3. Предусловия
4. Шаги и входные данные
5. Ожидаемый результат
6. Постусловия (если нужно)
7. Прикреплённая баг-ссылка (если упал)

---

### Советы

- Пиши понятно и коротко — документация должна быть читаемой для любого члена команды
- Не дублируй кейсы — группируй по целям
- Не ленись фиксировать граничные и необычные случаи
- Оформляй шаблоны (в Notion/Jira/Confluence/Google Docs, TestRail, Zephyr и др.)
- Не храни кейсы только в голове!

---

**Задание для практики:**
- Оформить 3 тест-кейса и чек-лист для одной функции (например, `calc_tax`)
- Подумать, какие баги могли бы встречаться, и пример баг-репорта

---

### Пример оформления — решение

#### 1. Тест-кейсы

| № | Действие                | Вход     | Ожидание     |
|---|-------------------------|----------|--------------|
| 1 | Доход меньше 1000       | 800      | 150          |
| 2 | Доход на границе        | 1000     | 160          |
| 3 | Доход выше 10000        | 12000    | 750          |

---

#### 2. Чек-лист по функции `calc_tax`

- [ ] Проверить, что доход <1000 возвращает 150
- [ ] Доход = 1000 — первая сотня, возвращает 160
- [ ] Доход = 9999 — много сотен, возвращает 1050
- [ ] Доход >10000 — процент, возвращает 150 + 5% от дохода
- [ ] Корректно обрабатывает отрицательные значения
- [ ] Корректно работает с числами float
- [ ] Ошибку на строковых/некорректных данных

---

#### 3. Пример баг-репорта

**Название**: Некорректный расчет налога для float

**Шаги:**
1. Ввести доход 1050.5 в функцию `calc_tax`
2. Получить результат

**Ожидание:**
- Функция корректно высчитывает количество сотен (одна сотня), итог: 160

**Фактическое:**
- Возвращает 150 (ошибка вычисления: пропущен дробный доход)

**Серьёзность:** Средняя

**Среда:** Python 3.11, Windows 10

**Комментарий:**  
Ошибка возникает при вводе дробного значения дохода — функция игнорирует десятые/сотые, хотя должна округлять вверх до полной сотни.

---

#### Вариант записи тест-кейса в системе типа TestRail

**Test Case:** Проверка процентного налога при доходе больше 10000  
**Precondition:** Пользователь указывает доход  
**Steps:**
1. Ввести доход 15000
2. Запустить функцию  
**Expected:** Налог = 900 (150 + 750)

---

🐞 Жизненный цикл бага
📌 Основные этапы

New / Open (Создан) — тестировщик нашёл баг и создал репорт.

Triage / To Triage — баг оценивают: валидность, приоритет, серьёзность, воспроизводимость.

Assigned / To Do — назначен ответственному разработчику или команде.

In Progress — разработчик работает над исправлением.

Fixed / Dev Done — разработчик сделал фикс, прошёл код-ревью.

Ready for QA / Ready for Testing — задача возвращается тестировщику.

Verified — тестировщик подтвердил, что фикс корректный.

Closed / Resolved — баг закрыт.

Reopened — если проблема всё ещё воспроизводится.

Дополнительные статусы:

Duplicate — дубликат.

Won’t Fix / Not a bug — не будет исправлено.

Deferred — отложено.

Cannot Reproduce — не воспроизводится.

👥 Роли в процессе

Тестировщик (QA):

создаёт баг, описывает шаги, окружение, логи, данные;

ставит preliminary severity;

верифицирует исправление.

Тимлид / PO / QA Lead:

проводит триаж, выставляет приоритет;

принимает решение о включении бага в релиз.

Разработчик:

анализирует и исправляет баг;

пишет комментарии и передаёт на тестирование.

DevOps / Release Manager:

участвует, если баг связан с инфраструктурой/деплоем.

🔥 Severity vs Priority
Severity (Серьёзность) — техническое влияние:

Critical — падения, потеря данных

Major — ключевая функциональность нарушена

Minor — некорректное поведение, есть обходной путь

Trivial — косметика

Priority (Приоритет) — насколько быстро исправлять:

P0 — фикс немедленно

P1 — высокий, в ближайший релиз

P2 — важный, но не срочный

P3/P4 — можно отложить

📝 Чеклист идеального баг-репорта

Title: компонент + короткое описание

Описание: что ожидалось / что получили

Шаги воспроизведения (пронумерованные)

Expected result

Actual result

Окружение: env, сервис, версия, коммит

Логи / stacktrace / request-response

Скриншоты / дампы / SQL / Kafka-сообщения

Severity / Priority

Test data

Reproducibility

Assignee / Labels

📦 Пример баг-репорта (markdown)
**Title:** [API/payments-service] Создание платежа возвращает 500 при amount=0

**Environment:**
- env: staging
- service: payments-api v1.23.4 (commit abcdef)
- DB: PostgreSQL 13
- Kafka: staging cluster

**Steps to reproduce:**
1. Авторизоваться под user_id=1001.
2. POST /api/v1/payments { "amount": 0, "currency": "RUB", "to_account": "123" }
3. Посмотреть ответ сервера.

**Expected:**
- HTTP 400 Bad Request — "amount must be > 0".

**Actual:**
- HTTP 500 Internal Server Error  
- {"error": "NullPointerException at ..."}

**Reproducibility:** 100%

**Attachments:**
- curl-запрос  
- server log  
- stacktrace  

**Severity:** Major  
**Priority:** P1

**Notes:**
- В логах NPE в PaymentValidator.validateAmount.
- Workaround: использовать amount >= 1.

🧪 Как проверить фикс (QA checklist)

Поднять окружение с фиксом

Повторить шаги воспроизведения

Проверить смежные сценарии (регрессия)

Проверить логи, данные в БД, Kafka-сообщения

Убедиться, что нет побочных эффектов

Закрыть баг и привязать к релизу

Для Critical багов — убедиться, что фикс в проде

❓ Частые вопросы на собеседовании
1. Что делать, если разработчик говорит, что баг не воспроизводится?

сравнить окружения

приложить точные запросы/логи

дать HAR / curl

попробовать воспроизвести вместе

2. Когда ставится статус «Cannot Reproduce»?

если шаги корректны, но ни на одном окружении проблема не повторяется

3. Как определяете severity и priority?

severity — техническая критичность

priority — бизнес-влияние (PO решает)

4. Что делать с flaky?

изоляция причины

дополнительное логирование

стабилизация тестовых данных

временный skip + отдельная задача